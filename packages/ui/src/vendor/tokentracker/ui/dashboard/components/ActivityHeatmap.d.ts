import type {ComponentType} from 'react';
export const ActivityHeatmap: ComponentType<{
  heatmap?: unknown;
  timeZoneLabel?: string;
  timeZoneShortLabel?: string;
  hideLegend?: boolean;
  embedded?: boolean;
}>;
