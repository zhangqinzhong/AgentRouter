import type {ComponentType} from 'react';
import type {ProviderUsage} from '../../../lib/model-breakdown';
export const CostAnalysisModal:ComponentType<{isOpen:boolean;onClose:()=>void;fleetData:ProviderUsage[]}>;
