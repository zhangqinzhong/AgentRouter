# AgentRouter 1.8.0

## 界面

- 设置、供应商、Agent 配置档案改成分组列表，不再用撑满窗口的空表，也不再重复页面标题。
- 热力图「使用最多的工具」使用 VS Code Icons 彩色图标，进度条按工具分色，打开页面时从 0 涨到实际比例。

## 请求日志

- 单网关链路补齐流式指标：首文本等待、输出间隔和测速样本状态会从采集写入原始追踪，再存进请求日志。
- 没有这些时间点的旧日志保持原样，不会事后补算。

macOS 安装包继续采用 ad-hoc 本地签名，未经过 Apple 公证。
